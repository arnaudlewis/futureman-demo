- var whoWeAre = slice.value.toArray()[0]

section.content-2.who-we-are
  .container
    .row
      .col-xs-10.col-sm-12.col-xs-offset-1.col-sm-offset-0
        svg.line(xmlns='http://www.w3.org/2000/svg', xmlns:xlink='http://www.w3.org/1999/xlink')
          line(x1='0', y1='5', x2='1000', y2='5')
        != whoWeAre.getStructuredText('title1').asHtml(ctx.linkResolver)
        != whoWeAre.getStructuredText('subTitle').asHtml(ctx.linkResolver)
    .row
      .col-xs-10.col-sm-6.col-xs-offset-1.col-sm-offset-0
        != whoWeAre.getStructuredText('leftText').asHtml(ctx.linkResolver)
      .col-xs-10.col-sm-6.col-xs-offset-1.col-sm-offset-0
        != whoWeAre.getStructuredText('rightText').asHtml(ctx.linkResolver)
